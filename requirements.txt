# Install the local package plus any runtime packages not declared in the package's pyproject
# Installing the editable package will pull the dependencies declared in
# message_broker/pyproject.toml; we list runtime extras here that are used
# by the code but not declared in that file.

-e ./message_broker
redis>=4.6.0
aio-pika>=9.4.0